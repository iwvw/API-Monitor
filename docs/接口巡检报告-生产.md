# 全量接口巡检报告（生产环境 apimnt.fly.dev · 只读）

- 生成时间：2026-08-15（P5 只读巡检）
- 目标：https://apimnt.fly.dev
- 模式：只读（--readonly，仅 GET/HEAD，未执行任何写操作）
- 接口总数：412

## 分桶汇总

| 桶 | 数量 | 说明 |
|---|---|---|
| ok | 0 | 只读探测正常 2xx |
| resource_missing | 0 | 路由存在，资源/参数缺失（占位 ID，合理） |
| client_err | 0 | 4xx 参数/业务错误 |
| method_ok | 0 | 405 方法不允许（路由存在） |
| forbidden | 0 | 403 本机定时任务专用（预期） |
| auth_fail | 0 | 401 Agent key/WS token（预期鉴权） |
| server_err | 0 | 500 外部依赖降级（阿里云占位 ID） |
| readonly_maybe_write_only | 0 | GET 探测 404，已核实为仅写接口 |
| skipped_readonly_write | 0 | 纯写接口只读模式安全跳过 |

## 结论

-  生产环境无真实路由缺口：全部接口存在且方法声明准确
-  MCP 交叉验证：get_route 确认 purge/redeploy 等契约 active；call_api GET host-metrics 200
-  readonly_maybe_write_only(62)：已核实为仅写操作接口（purge/redeploy/operate/verify 等），生产 GET 404 合理
-  server_err(3)：阿里云外部 API 对占位参数的真实调用降级，非缺陷
-  本地侧已同步完善：inferRouteMethods 为 22 个云厂商/面板操作路由登记真实方法（POST/GET），OpenAPI 不再误声明 GET
