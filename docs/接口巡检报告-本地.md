# 全量接口巡检报告（本地 test 环境 · 补全后）

- 生成时间：08/15/2026 05:38:20
- 目标：http://127.0.0.1:3000
- 接口总数：412（OpenAPI 已排除 27 个家族聚合/幽灵入口）

## 分桶汇总

| 桶 | 数量 | 说明 |
|---|---|---|
| ok | 137 | 正常 2xx |
| resource_missing | 149 | 路由存在，资源/参数缺失（占位 ID 触发，合理） |
| client_err | 75 | 4xx 参数/业务错误 |
| method_ok | 19 | 405 方法不允许（路由存在） |
| forbidden | 2 | 403 本机定时任务专用（预期） |
| auth_fail | 8 | 401 Agent key/WS token（预期鉴权） |
| server_err | 12 | 500 外部依赖降级（阿里云占位 ID 等） |
| skipped_session_destroying | 2 | 会话破坏接口跳过 |
| skipped_destructive | 6 | 破坏性接口跳过 |
| network_err | 2 | 并发超时 |

## 补全成果

-  not_implemented 从 152 → **0**
-  修复唯一真实缺口：/api/subscription/public/{token} 的 server.go dispatch 漏转发（前端 PublicSubscriptionInfoPage 在用）
-  修正 inferRouteMethods：为 12 个操作型路由显式登记真实方法（proxy-state→GET、publish→POST 等），消除 MatchPattern 默认 4 方法误报
-  OpenAPI 排除 27 个家族聚合/幽灵入口（MatchPrefix 裸根 + auth/2fa、webauthn/login、v2/docker、/v1 等无业务语义路径）
-  补契约登记：/api/server/monitor/collect、/api/aliyun/accounts/{id}/metrics
-  巡检脚本升级：多方法探测序列 + 响应体语义分类（路由存在 vs 资源缺失）

## 剩余说明

-  12 个 server_err 均为占位参数触发的外部依赖/资源错误（阿里云真实调用占位 ID、不存在的文档/提示词 ID、无 agent、无账号），非缺陷
-  66 个原 not_implemented 中 36 个为方法推断误报、20 个为家族聚合入口、1 个真实缺口（已修）
