# 超大文件重构结果报告

> 分支：`refactor/large-files`（从 `dev` 的 `5e903e03` 切出）
> 状态：**待人工确认合并**（未合入 `dev`/`main`）

## 一、总览

超 1000 行业务文件：**66 → 42**；超 3000 行巨型文件：**10 → 1**。

| 规模档 | 前 | 后 |
|---|---|---|
| > 3000 行 | 10 | 1（ServerPage，见遗留项） |
| 2000–3000 行 | 16 | 5 |
| 1000–2000 行 | 40 | 37 |
| 合计 > 1000 | 66 | 42 |

- 前端 24 个超大页面全部拆入 `src/js/pages/<module>/`（方案 A）
- 后端 8 个包巨石文件全部按功能簇拆为同包多文件
- 全部为**行为零变更**（逐字搬移 + import 调整）

## 二、执行批次

### 后端 B1–B8
- B1 叶子工具：subscription(uri_parse/mihomo_validate)、uptime(probe/jsonpath)、cloudflare(transport/helpers)、notification(helpers)
- B2 schema：6 包各建 schema.go
- B3 CRUD：cloudflare 8 / notification 10 / m365 14 文件
- B4 openai：relay→5、service→6、endpoints→5、proxypool→7
- B5 adminai：engine.go → 8 文件
- B6 uptime 运行态：service.go → 14 文件（锁契约保持）
- B7 subscription 主体：service.go → 15 文件（计费周期红线保持）
- B8 serveragent：service.go→7、server_ops.go→4、managed_forwards.go→3、managed_proxy_nodes.go→2（New() 与三闭包 SHA256 一致）

### 前端 B9–B12
- B9：Filebox 试点 + 20 页（RemoteDesktop/Oracle/Notification/Scheduler/Uptime/Settings/Gcp/PublicGitHub/Totp/ApiDocs/GitHub/Subscription 等）
- B10：OpenAIPage/M365Page/DnsPage/PaasPage
- B11：HuaweiPage 深化；components/tiles → pages/tiles；components/adminai 原地重组；useEndpoints 拆 3 子 hook
- B12：ServerPage 提取常量/工具/21 展示组件到 pages/server/

每个原页面保留 1 行转发文件，旧 import 路径持续可用。

## 三、一致性验证

### 3.1 基线快照对比
- 基线 `docs/refactor-baseline/`（路由 373 + 15 只读接口 + 6 静态资源 + dist 入口指纹）
- 工具 `tools/refactor-snapshot.mjs` / `refactor-snapshot-diff.mjs`
- 每批次及最终复采比对，**全部 PASS（零非预期差异）**

### 3.2 路由契约
- 最终 373 条，与基线逐条一致（零漂移）

### 3.3 代码级核验
- 搬移内容逐字节 / AST 级比对，函数体一字未改
- 红线保持：reserveManagedProxyPort、planCycleWindow、uptime per-monitor 锁、serveragent New() 闭包
- 修复偏差：B3 validateSourceModule 归位、B12 areServerSnapshotsEqual 标识符

### 3.4 全量门禁
audit:fast / go build / go vet / go test（零 FAIL）/ npm test（413）/ ui:governance / governance:check / docs:check —— 全部通过。

### 3.5 关键流程回归
鉴权边界（401/200）、登录（脱敏）、静态资源 —— 符合预期。

## 四、遗留项（需后续处理）

1. **ServerPage.jsx 仍 10306 行**：已安全提取常量/工具/21 展示组件（11428→10306）。剩余 6 个 tab 区块（terminal/docker/management 等）深度依赖主组件闭包，提取需传递 60–100 个 props，风险超出「行为零变更」收益，故停止。建议后续单独任务处理。
2. **后端 1000–2000 行区间的部分文件**（filebox/settings/totp/system/github/koyeb 等 service.go）未在本批拆分范围，属次优先。
3. **拆分后的容器文件**（paas/PaasPage 2911、dns/DnsPage 2044、uptime/UptimePage 1477 等）按职责拆分后仍较大，可继续按 tab 下沉。
4. **注释丢失（零行为影响）**：OpenAIPage 11 行、PaasPage 12 行说明性注释未随组件迁移。

## 五、合入方式

经人工确认后：
```
git checkout dev
git merge --no-ff refactor/large-files
```
AI 不自行合并，不推送 main。