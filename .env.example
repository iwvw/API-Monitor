# ===================================
# API Monitor 环境变量模板
# 使用方法: 复制此文件为 .env 并根据需要修改
# ===================================

# --- 基础设置 ---
# 服务端口
PORT=3000
# 运行环境 (development 或 production)
NODE_ENV=production
# 管理员初始密码 (仅在数据库为空时生效)
ADMIN_PASSWORD=admin123456
# 会话密钥 (建议设置一个长字符串以固定登录状态)
JWT_SECRET=your_random_secret_string_here

# --- 日志设置 ---
# 日志级别 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO
# 日志保留天数
LOG_RETENTION_DAYS=7

# --- 数据目录 ---
# 数据库和日志存放路径 (Docker 环境下建议保持默认)
DATA_DIR=/app/data

# --- 预置账号 (可选) ---
# 格式: "名称:Token,名称2:Token2"
# ACCOUNTS=MyZeabur:your_token_here,OtherAcc:other_token_here

# --- 高级网络设置 (通常不需要) ---
# 是否信任代理 (如果你使用 Nginx 反向代理，建议设为 true)
TRUST_PROXY=true
